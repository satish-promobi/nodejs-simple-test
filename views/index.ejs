<!DOCTYPE html>
<html ng-app="app">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1">
		<title>Luma Health - It's About Time</title>
		<link href='//fonts.googleapis.com/css?family=Open+Sans' rel='stylesheet' type='text/css'>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/css/bootstrap.min.css">
	</head>
    <body>
        <h1>Welcome to Luma Health!</h1>
        <p>Please sign up below:</p>
        <div class="page-header" style="text-align: center">
            <h3>Contact Us</h3>
        </div>
        <div class="container-fluid" data-ng-controller="ContactUsController">
            <div class="row">
                <div class="col-md-12">
                    <form name="contactUsForm" class="form-horizontal" data-ng-submit="create()" novalidate>
                        <fieldset>
                            <div class="form-group"
                                 data-ng-class="{'has-error': contactUsForm.$submitted || (contactUsForm.name.$invalid && contactUsForm.name.$dirty) }">
                                <label for="name" class="col-sm-2 control-label">Name: </label>
                                <div class="col-sm-10">
                                    <input name="name" data-ng-model="name" type="text" id="name" class="form-control"
                                           placeholder="Please enter your name" required/>
                                    <span class="help-block" data-ng-show="contactUsForm.$submitted || (contactUsForm.name.$invalid && contactUsForm.name.$dirty) ">
                                        * Name is required
                                    </span>
                                </div>
                            </div>
                            <div class="form-group"
                                 data-ng-class="{'has-error': contactUsForm.$submitted || (contactUsForm.email.$invalid && contactUsForm.email.$dirty) }">
                                <label for="email" class="col-sm-2 control-label">Email: </label>
                                <div class="col-sm-10">
                                    <input name="email" data-ng-model="email" type="email" id="email" class="form-control"
                                           placeholder="Please enter your email" required/>
                                    <span class="help-block"
                                          data-ng-show="contactUsForm.$submitted || (contactUsForm.email.$invalid && contactUsForm.email.$dirty)">
                                        * Email is required
                                    </span>
                                    <span class="help-block"
                                          data-ng-show="contactUsForm.email.$error.email">This is not a valid email.</span>
                                </div>
                            </div>
                            <div class="form-group"
                                 data-ng-class="{'has-error': contactUsForm.$submitted || (contactUsForm.subject.$invalid && contactUsForm.subject.$dirty)}">
                                <label for="subject" class="col-sm-2 control-label">Subject: </label>
                                <div class="col-sm-10">
                                    <input name="subject" data-ng-model="subject" type="text" id="subject" class="form-control"
                                           placeholder="Please enter subject" required/>
                                    <span class="help-block"
                                          data-ng-show="contactUsForm.$submitted || (contactUsForm.subject.$invalid && contactUsForm.subject.$dirty)">
                                        * Subject is required
                                    </span>
                                </div>
                            </div>
                            <div class="form-group"
                                 data-ng-class="{'has-error': contactUsForm.$submitted || (contactUsForm.message.$invalid && contactUsForm.message.$dirty)}">
                                <label for="message" class="col-sm-2 control-label">Message: </label>
                                <div class="col-sm-10">
                                    <textarea name="message" data-ng-model="message" type="text" id="message" class="form-control"
                                              col="30" rows="10" required></textarea>
                                    <span class="help-block"
                                          data-ng-show="contactUsForm.$submitted || (contactUsForm.message.$invalid && contactUsForm.message.$dirty)">
                                        * Message is required
                                    </span>
                                </div>
                            </div>
                            <div class="form-group">
                                <div class="col-sm-offset-2 col-sm-10">
                                    <button type="submit" class="btn btn-primary" data-ng-disabled="contactUsForm.$invalid">Submit</button>
                                </div>
                            </div>

                            <!--<div data-ng-show="error" class="text-danger">-->
                                <!--<strong data-ng-bind="error"></strong>-->
                            <!--</div>-->
                        </fieldset>
                    </form>
                </div>
            </div>
        </div>

        <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.2/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.4/js/bootstrap.min.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.15/angular.min.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.15/angular-route.min.js"></script>
        <script src="https://ajax.googleapis.com/ajax/libs/angularjs/1.3.15/angular-resource.min.js"></script>
        <script src="public/modules/app.js"></script>
    </body>
</html>